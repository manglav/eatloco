<h1>Dishes Directory</h1>

<table>
  <tr>
    <th>Dish Name</th>
    <th>Empty Field 1</th>
    <th>Empty Field 2</th>
  </tr>

<% @dishes.each do |dish| %>
  <tr>
    <td><%= link_to dish.name, dish %></td>
    <td><%= link_to 'Edit', edit_dish_path(dish) %></td>
    <td><%= link_to 'Destroy', dish, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Dish', new_dish_path %>
