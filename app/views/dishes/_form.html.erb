<%= simple_form_for(@dish) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
		<%= f.input :name, label: 'Name of the Dish', hint: 'Between 6 and 12 characters please' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
